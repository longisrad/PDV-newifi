name: Build Padavan NEWIFI3

on:
  workflow_dispatch: # Cho phép chạy thủ công từ tab Actions
  push:
    branches:
      - main
      - master
    paths:
      - 'NEWIFI3.config' # Tự động chạy khi file config thay đổi

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          path: rt-n56u-src # Tải mã nguồn từ chính repo của bạn về thư mục tạm

      - name: Move repository to /opt/rt-n56u
        run: |
          sudo mkdir -p /opt/rt-n56u
          sudo chown -R runner:runner /opt/rt-n56u
          # Di chuyển toàn bộ mã nguồn của bạn sang thư mục /opt/rt-n56u để khớp đường dẫn hệ thống
          mv rt-n56u-src/* /opt/rt-n56u/ || true
          mv rt-n56u-src/.* /opt/rt-n56u/ 2>/dev/null || true

      - name: Initialize Environment
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo apt-get update
          sudo apt-get install -y unzip libtool-bin curl cmake gperf gawk flex bison nano xxd fakeroot \
            cpio git python3-docutils gettext automake autopoint texinfo build-essential help2man \
            pkg-config zlib1g-dev libgmp3-dev libmpc-dev libmpfr-dev libncurses5-dev libltdl-dev wget libc-dev-bin \
            upx-ucl
          
          # Thiết lập fakeroot-sysv làm mặc định để biên dịch ổn định trên Ubuntu 22.04
          sudo update-alternatives --set fakeroot /usr/bin/fakeroot-sysv || true

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'
          cache: false # Tắt cảnh báo cache của Go

      - name: Download and compress AdGuardHome binary
        run: |
          # Download bản mipsle_softfloat mới nhất từ AGH releases
          AGH_URL=$(curl -s https://api.github.com/repos/AdguardTeam/AdGuardHome/releases/latest \
            | grep "browser_download_url" \
            | grep "mipsle_softfloat.tar.gz" \
            | cut -d '"' -f 4)
          echo "Downloading AGH from: $AGH_URL"
          wget -q -O /tmp/AdGuardHome_linux_mipsle_softfloat.tar.gz "$AGH_URL"

          # Extract binary
          mkdir -p /tmp/agh_extract
          tar -xzf /tmp/AdGuardHome_linux_mipsle_softfloat.tar.gz -C /tmp/agh_extract
          AGH_BIN=$(find /tmp/agh_extract -name "AdGuardHome" -type f | head -1)
          echo "Found binary: $AGH_BIN ($(du -h $AGH_BIN | cut -f1))"

          # UPX compress - dùng --lzma cho chip MIPS để nén file tối đa
          upx --lzma "$AGH_BIN" || upx "$AGH_BIN" || echo "UPX failed, using uncompressed"
          echo "After UPX: $(du -h $AGH_BIN | cut -f1)"

          # Copy vào đúng vị trí trong thư mục mã nguồn
          cp "$AGH_BIN" /opt/rt-n56u/trunk/user/adguardhome/AdGuardHome
          chmod +x /opt/rt-n56u/trunk/user/adguardhome/AdGuardHome
          echo "AGH binary placed at trunk/user/adguardhome/AdGuardHome"

      - name: Prepare Config
        run: |
          # Áp dụng file cấu hình NEWIFI3.config ngoài thư mục gốc vào các vị trí templates của mã nguồn
          cp -f /opt/rt-n56u/NEWIFI3.config /opt/rt-n56u/trunk/configs/templates/NEWIFI3.config || true
          cp -f /opt/rt-n56u/NEWIFI3.config /opt/rt-n56u/trunk/configs/templates/NEWIFI-D2.config || true
          cp -f /opt/rt-n56u/NEWIFI3.config /opt/rt-n56u/trunk/configs/templates/newifi3.config || true
          cp -f /opt/rt-n56u/NEWIFI3.config /opt/rt-n56u/trunk/.config

      - name: Build Toolchain from Source
        run: |
          cd /opt/rt-n56u/toolchain-mipsel
          if [ -f "./clean_toolchain" ]; then
            ./clean_toolchain
          elif [ -f "./clean_sources" ]; then
            ./clean_sources
          fi
          
          if [ -f "./build_toolchain" ]; then
            ./build_toolchain
          elif [ -f "./build_toolchain_3.4.x" ]; then
            ./build_toolchain_3.4.x
          else
            echo "No build_toolchain found!"
            exit 1
          fi

      - name: Build Firmware
        run: |
          cd /opt/rt-n56u/trunk
          ./clear_tree
          
          if [ -f "./build_firmware_modify" ]; then
            fakeroot ./build_firmware_modify NEWIFI3
          elif [ -f "./build_firmware" ]; then
            fakeroot ./build_firmware
          else
            echo "No build script found!"
            exit 1
          fi

      - name: Prepare Release Artifacts
        id: prepare_files
        run: |
          echo "release_tag=NEWIFI3-$(date +'%Y.%m.%d-%H%M')" >> $GITHUB_OUTPUT
          echo "compile_date=$(date +'%Y-%m-%d %H:%M')" >> $GITHUB_OUTPUT

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Padavan-NEWIFI3-Firmware
          path: |
            /opt/rt-n56u/trunk/images/*.trx
            /opt/rt-n56u/trunk/images/*.md5

      - name: Create Release
        uses: softprops/action-gh-release@v2
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        with:
          tag_name: ${{ steps.prepare_files.outputs.release_tag }}
          name: NEWIFI3 Padavan Firmware [${{ steps.prepare_files.outputs.compile_date }}]
          body: |
            ### Padavan Firmware
